services:
  mit-panel:
    build: .
    image: mit-panel:latest
    container_name: mit-panel
    restart: always
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
    env_file:
      - ./.env
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
