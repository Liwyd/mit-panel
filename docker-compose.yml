services:
  nexra-panel:
    build: .
    image: nexra-panel:latest
    container_name: nexra-panel
    restart: always
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
    env_file:
      - ./.env
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
